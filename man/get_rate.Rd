% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_rate.R
\name{get_rate}
\alias{get_rate}
\title{Get life-history rate based on temperature or photoperiod}
\usage{
get_rate(temp = NULL, photoperiod = NULL, species, rate)
}
\arguments{
\item{temp}{Numeric vector or \code{\link[terra:SpatRaster-class]{terra::SpatRaster()}} with temperature values (°C).}

\item{photoperiod}{Numeric vector or \code{\link[terra:SpatRaster-class]{terra::SpatRaster()}} with photoperiod values (hours).}

\item{species}{Character. Species name. One of: \code{"aegypti"}, \code{"albopictus"}, \code{"koreicus"}, \code{"japonicus"}.}

\item{rate}{Character. One of:
\itemize{
\item \code{"a.surv"}: Adult daily survival rate
\item \code{"a.gono"}: Gonotrophic cycle rate (1/duration)
\item \code{"a.ovi"}: Oviposition rate (eggs per female/day)
\item \code{"i.surv"}: Immature daily survival rate
\item \code{"i.emer"}: Immature daily emergence rate
\item \code{"i.ddmort"}: Immature density-dependent mortality (based on density, not temperature)
\item \code{"e.hatch"}: Hatching rate of eggs
\item \code{"e.surv"}: Egg daily survival rate
\item \code{"d.surv"}: Diapausing egg daily survival rate
\item \code{"e.diap"}: Diapause allocation (based on photoperiod)
}}
}
\value{
A numeric vector (if input is numeric) or \code{\link[terra:SpatRaster-class]{terra::SpatRaster()}} if raster input.
}
\description{
Computes one of the internal temperature- or photoperiod-dependent life-history rates
for a given mosquito species. Supports numeric vectors and \code{\link[terra:SpatRaster-class]{terra::SpatRaster()}} inputs.
}
\examples{
\dontrun{
# Example 1: Vector of temperatures
temps <- seq(-5, 40, by = 1)
x <- get_rate(temp = temps, species = "aegypti", rate = "a.surv")

# Example 2: SpatRaster temperature
r <- terra::rast(nrows = 10, ncols = 10, nlyrs = 1)
values(r) <- runif(ncell(r), 10, 35)
r_rate <- get_rate(temp = r, species = "albopictus", rate = "a.gono")
plot(r_rate)

# Example 3: Spatiotemporal SpatRaster for photoperiod
photo_r <- terra::rast(nrows = 5, ncols = 5, nlyrs = 3)
values(photo_r) <- rep(c(13, 12, 11), each = ncell(photo_r))
time(photo_r) <- as.Date("2020-06-01") + 0:2
diap_stack <- get_rate(photoperiod = photo_r, species = "albopictus", rate = "e.diap")
plot(diap_stack)
}
}
\author{
Matteo Marcantonio \email{marcantoniomatteo@gmail.com}, Daniele Da Re \email{dare.daniele@gmail.com}
}
